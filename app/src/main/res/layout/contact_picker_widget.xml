<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="@dimen/list_widget_horizontal_padding"
    android:paddingEnd="@dimen/list_widget_horizontal_padding"
    android:paddingTop="@dimen/list_widget_vertical_padding"
    android:paddingBottom="@dimen/list_widget_vertical_padding"
    android:clipToPadding="false">

    <CheckBox
        android:id="@+id/check_box"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <androidx.appcompat.widget.AppCompatEditText
        android:id="@+id/search_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/list_widget_horizontal_padding"
        android:padding="@dimen/padding_small"
        android:background="@drawable/search_background"
        android:singleLine="true"
        android:drawableStart="@drawable/ic_search"
        android:drawablePadding="8dp"
        android:elevation="@dimen/list_widget_elevation"
        android:hint="@string/search_hint"
        android:enabled="false"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/check_box"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
